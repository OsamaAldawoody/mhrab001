<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.Leanback">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:lineSpacingMultiplier">1.0</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorActivatedHighlight">@color/back_text</item>
        <item name="android:colorAccent">@color/back_text</item>
        <item name="android:colorControlNormal">@color/back_text</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppThemeAct" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">false</item>
    </style>

    <style name="MyCustomTheme">
        <item name="android:windowAnimationStyle">@style/MyAnimation.Window</item>
        <item name="android:windowBackground">@color/commentDialogBackground</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="MyAnimation.Window" parent="@android:style/Animation.Activity">
        <item name="android:windowEnterAnimation">@anim/slide_in_up_dialog</item>
        <item name="android:windowExitAnimation">@anim/slide_out_down_dialog</item>
    </style>

</resources>
